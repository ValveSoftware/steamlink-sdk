include_rules = [
  "+gpu/command_buffer/client",
  "+gpu/config",
  "+gpu/ipc/client",
  "+gpu/ipc/common",
  "+services/ui/gpu/interfaces",
]

specific_include_rules = {
  "gpu_service_proxy.h": [
    "+services/ui/gpu/gpu_main.h",
  ],
}
