include_rules = [
  "+gpu/config",
  "+gpu/command_buffer",
  "+gpu/ipc/client",
  "+gpu/ipc/common",
]

specific_include_rules = {
  "run_all_service_tests\.cc": [
    "+mojo/edk/embedder/embedder.h",
  ],
}
