#include <QtCore>

class MyInterface
{
    ENUM Enum1 { First, Second, Third }
    PROP(Enum1 enum1 = First)

    PROP(bool started = false)

    SLOT(bool start())
    SLOT(bool stop())

    SIGNAL(advance())
};
