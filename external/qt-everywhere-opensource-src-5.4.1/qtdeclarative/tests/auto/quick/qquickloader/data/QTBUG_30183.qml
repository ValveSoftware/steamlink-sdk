import QtQuick 2.0

Loader {
    width: implicitWidth
    height: implicitHeight

    sourceComponent: Rectangle {
        color: "green"
        implicitWidth: 240
        implicitHeight: 120
    }
}
