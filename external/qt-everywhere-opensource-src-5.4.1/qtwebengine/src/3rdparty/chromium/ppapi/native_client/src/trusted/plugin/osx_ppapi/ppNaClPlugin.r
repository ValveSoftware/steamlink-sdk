#include <CoreServices/CoreServices.r>

resource 'STR#' (126) { {
  "PPAPI NativeClient",
  "PPAPI NativeClient Plug-in"
} };

resource 'STR#' (127) { {
  "PPAPI NativeClient Module - SRPC",
} };

resource 'STR#' (128) { {
  "application/x-nacl",
  "nexe",
} };
